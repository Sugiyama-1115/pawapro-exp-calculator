# 利用マニュアル（起動方法・使い方）

パワプロ（eBASEBALLパワフルプロ野球）サクセスモードの育成選手について、完成までに必要な経験点（筋力・敏捷・技術・変化球・精神）を計算するアプリの利用マニュアル。

対応バージョン: eBASEBALLパワフルプロ野球2024-2025（パワフルフューチャーズ）
対応環境: PC の Chromium系ブラウザ最新版（Google Chrome / Microsoft Edge 等）のみ。スマートフォン非対応。

---

## 1. 起動方法

このアプリはサーバーを使わず、**すべてブラウザ内で完結**する（外部通信は一切行わない）。起動方法は2通りある。

### 方法A: GitHub Pages で開く（インストール不要・推奨）

`main` ブランチに push すると GitHub Actions が自動でビルド・公開する。ブラウザで以下の URL を開くだけでよい。

```text
https://sugiyama-1115.github.io/pawapro-exp-calculator/
```

- 初回公開までは数分かかる場合がある。公開状況は GitHub リポジトリの `Actions` タブで確認できる。
- この方法ではサンプルデータ（ダミー値）のみが同梱される。実測データを使うには後述の「4. 実測データCSVの準備」でアプリ上からインポートする。

### 方法B: ローカルで開発サーバーを起動する（毎回のCSVインポートが不要になる）

実測データCSVを `public/data/pawapro2024/` に一度置いてしまえば、以降はインポート操作なしで起動するたびに読み込まれる。頻繁に使う人はこちらを推奨。

**コマンドは必ずこのプロジェクトのフォルダ（`package.json` がある場所）で実行する。**

```bash
# リポジトリを未取得の場合、初回のみ clone してフォルダに入る
cd C:\Users\xxx\project
git clone git@github.com:Sugiyama-1115/pawapro-exp-calculator.git
cd pawapro-exp-calculator

# 既に clone 済みなら、プロジェクトフォルダに移動するだけでよい
cd C:\Users\xxx\project\pawapro-exp-calculator
```

上記でカレントディレクトリが `C:\Users\xxx\project\pawapro-exp-calculator`（`package.json` や `Instructions.md` が直下にあるフォルダ）になっていることを確認したうえで、以下を実行する。

```bash
# 初回のみ: 依存パッケージを導入
npm install

# 開発サーバーを起動
npm run dev
```

起動後、ターミナルに表示される URL（既定では `http://localhost:5173/`）をブラウザで開く。`Ctrl+C` で停止する。

> `npm install` や `npm run dev` を別のフォルダ（例: `public/data/` の中や、Cドライブ直下など）で実行すると `package.json が見つかりません` 等のエラーになる。必ずプロジェクト直下で実行すること。

---

## 2. 初回起動時の画面

起動すると、既定でサンプルデータ（`sample2024`）が読み込まれる。画面上部に黄色い注意バッジで「サンプルデータ（実際の値ではありません）」と表示される。これは動作確認・操作練習用のダミー値であり、実際のゲームの数値ではない。

実際の数値で計算したい場合は「3. データの用意」を参照。

---

## 3. データの用意（実測データCSVの準備）

ゲーム固有の数値（能力値ごとの必要経験点など）はすべて CSV ファイルとして管理する。**アプリのコードには数値を一切埋め込まない設計**のため、CSVを用意しないと計算はできない（サンプルデータでの動作確認は可能）。

### 3.1 データを用意する2つの方法

| 方法 | 手順 | 向いているケース |
|---|---|---|
| A. アプリ上からインポート | タブ1「選手設定」内の「CSVインポート」パネルからファイルを1種類ずつアップロード | GitHub Pages版を使う場合。都度アップロードが必要（ブラウザに保存され次回起動時も保持される） |
| B. `public/data/` に直接配置 | プロジェクトフォルダ（`C:\Users\xxx\project\pawapro-exp-calculator`）内の `public/data/pawapro2024/` フォルダを作り、CSVを置く | ローカル開発で毎回のインポートを省略したい場合、または全員が使う共有データとして固定したい場合 |

### 3.2 CSVの中身の作り方

1. `public/data/_template/` フォルダのテンプレート一式をコピーする。
2. `public/data/README.md`（記入ガイド）を読みながら値を埋める。
3. 完成したCSVをタブ1の「CSVインポート」からアップロードするか、`public/data/pawapro2024/` に配置する。

必要なファイルは以下の10種類（詳細は [public/data/README.md](public/data/README.md) を参照）。

- `config.csv`（センス倍率などの設定値）
- `base_ability_defs.csv`（基礎能力の定義：どの能力を何〜何まで表示するか）
- `base_normal.csv` / `base_sense_plus.csv`（基礎能力の必要経験点、センス○の有無で別ファイル）
- `blue_abilities.csv`（青特殊能力）
- `gold_abilities.csv`（金特殊能力）
- `gold_prerequisites.csv`（青特⇔金特の対応表）
- `hint_rules.csv`（コツLvごとの倍率）
- `breaking_cache_normal.csv` / `breaking_cache_sense_plus.csv`（変化球の実測キャッシュ、任意）

> データが全部揃っていなくてもアプリは動く。足りない箇所は「データ不足」として警告表示されるだけなので、**分かる範囲から少しずつ埋めていけばよい**。

---

## 4. 使い方（画面の流れ）

画面はヘッダー・5つのタブ・下部の結果サマリーバー（常時表示）で構成される。

```text
[ゲーム選択] [プラン一覧]
[1 選手設定] [2 基礎能力] [3 特殊能力] [4 変化球] [5 計算結果]
                 …タブの内容…
筋力 / 敏捷 / 技術 / 変化球 / 精神 / 合計   ステータス: 確定・推定含む・未完成
```

### タブ1: 選手設定

- 選手名、選手種別（投手 / 野手）、センス○の有無を設定する。
- CSVインポートパネルはここに折りたたみで配置されている。
- 選手種別を変えると入力済みの能力・特殊能力・変化球はクリアされる（確認ダイアログあり）。

### タブ2: 基礎能力

- 各能力ごとに「初期値」「目標値」を入力すると、必要経験点が自動計算される。
- 投手プランでは折りたたみで野手能力も入力できる。

### タブ3: 特殊能力

- 検索窓から青特・金特を探し「+ 追加」で選択中リストに入れる。
- 各能力ごとに「現在の状態→目標の状態」「コツLv」を指定する。
- 金特を追加すると、対応する下位青特が自動で追加される（🔗マーク）。

### タブ4: 変化球（投手プランのみ表示）

- 目標とする球種と変化量を追加する。
- 必要経験点は「一括入力」または「ステップごとに入力」のどちらかを選ぶ。データが無い場合は「未計測」警告が出る。

### タブ5: 計算結果

- 5カテゴリの合計・不足データの一覧・項目ごとの内訳を確認できる。
- 「結果をCSVで保存」「結果をJSONで保存」「プランをJSONで保存」からファイル出力できる（すべてブラウザのダウンロードとして保存され、外部には送信されない）。

### プラン一覧（ヘッダーの「プラン一覧」）

- 育成中の選手プランを複数保存・切替できる。新規作成・複製・削除・JSON読み込みが可能。
- 入力内容は 500ms 程度の間隔で自動的にブラウザ内（IndexedDB）に保存される。他の端末とは同期されない点に注意。

---

## 5. データの保存・入出力の仕組み

- すべてブラウザの **IndexedDB** に保存される（データベース名 `pawapro-exp-calculator`）。サーバーへは一切送信されない。
- ブラウザのプライベートモードやサイトデータ削除を行うと保存内容は消えるため、大事なプランは「プランをJSONで保存」で定期的にファイルへ書き出しておくことを推奨する。
- 別のPC・ブラウザに引き継ぐ場合は、書き出したプランJSONを「プラン一覧」→「JSONから読み込む」で取り込む。

---

## 6. トラブルシューティング

| 症状 | 原因・対処 |
|---|---|
| 「このゲームのデータが配置されていません」と出る | `pawapro2024` のCSVが未配置・未インポート。3章の手順でCSVを用意する。 |
| 経験点欄に「データがありません」の警告 | 対象区間のCSV行が未記入。分かる範囲で追記すればその都度反映される。 |
| CSVインポートでエラー一覧が出る | 1件でも検証エラーがあると全体が取り込まれない仕様。表示されたエラー内容（ファイル名・行番号）を確認し修正する。詳細は [public/data/README.md](public/data/README.md) §6。 |
| 保存できない・反映されない | ブラウザの保存容量不足の可能性。不要なプランを削除するか、警告バーの指示に従う。 |
| 画面が崩れる | 対応環境は最小幅1280pxのPC向けブラウザのみ。スマートフォン・小さいウィンドウ幅では非対応。 |

---

## 7. 開発者向けコマンド（参考）

普段の利用では不要。コードを変更・検証する場合のみ使用する。

```bash
npm run dev         # 開発サーバー起動
npm run build        # 型チェック + 本番ビルド
npm run test          # 単体テスト（Vitest）
npm run e2e           # E2Eテスト（Playwright）
npm run verify         # lint + 型チェック + 単体テスト（カバレッジ）+ E2E をまとめて実行
```

---

## 関連ドキュメント

- 詳細な画面仕様: [docs/05_ui_spec.md](docs/05_ui_spec.md)
- 保存・入出力の詳細仕様: [docs/06_persistence_spec.md](docs/06_persistence_spec.md)
- CSVデータの記入ガイド: [public/data/README.md](public/data/README.md)
- プロジェクト全体の構成: [PROJECT_ARCHITECTURE.md](PROJECT_ARCHITECTURE.md)

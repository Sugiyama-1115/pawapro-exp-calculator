# パワプロ サクセス必要経験点計算アプリ 仕様書

**文書バージョン：1.0**  
**初期対応タイトル：eBASEBALLパワフルプロ野球2024-2025**  
**初期対応シナリオ：パワフルフューチャーズ**  

---

# 1. 目的

本アプリは、サクセスで育成する選手について、

- 初期基礎能力
- 初期特殊能力
- 取得済み／取得予定のコツ
- 目標基礎能力
- 目標特殊能力
- 目標変化球

を入力することで、完成までに必要となる以下の経験点を計算する。

| 経験点種別 | 内部キー |
|---|---|
| 筋力 | `muscle` |
| 敏捷 | `agility` |
| 技術 | `technique` |
| 変化球 | `breaking` |
| 精神 | `mental` |

最終的に以下を表示する。

- 筋力経験点合計
- 敏捷経験点合計
- 技術経験点合計
- 変化球経験点合計
- 精神経験点合計
- 全経験点合計
- 基礎能力による消費内訳
- 青特殊能力による消費内訳
- 金特殊能力による消費内訳
- 変化球による消費内訳
- 実測値／推定値の区別
- 未入力・未登録データ

---

# 2. 基本設計思想

ゲーム固有の経験点データをプログラムコードへ直接記述しない。

計算アプリ本体とゲームデータを分離し、

**ゲーム固有データは原則CSVで管理する。**

これにより、新作発売時は原則としてCSVを差し替えるだけで対応可能な設計とする。

基本構造は以下とする。

```text
ゲームデータ（CSV）
        ↓
CSV Loader
        ↓
Calculation Engine
        ↓
選手作成画面
        ↓
必要経験点結果
```

計算エンジンは「パワプロ2024固有の数値」を知らない。

計算エンジンが知るのは、

```text
現在値
目標値
CSV上の経験点
コツLv
特殊能力の依存関係
```

のみとする。

---

# 3. 対応範囲

## 3.1 MVPで対応するもの

### 基礎能力

野手：

- 弾道
- ミート
- パワー
- 走力
- 肩力
- 守備力
- 捕球

投手：

- 球速
- コントロール
- スタミナ
- 守備力
- 捕球

必要に応じて投手の野手能力も入力可能なデータ構造とする。

### 特殊能力

- 青特殊能力
- ランク型青特殊能力
- 金特殊能力

### 変化球

- 第1球種
- 第2球種
- 第3球種以降
- 各球種の変化量
- 新球種取得
- 既存球種の変化量上昇

---

# 4. 対象外

以下はMVPでは自動計算対象外とする。

- 練習で獲得できる経験点予測
- 試合経験点予測
- イベント経験点予測
- ダイジョーブ博士成功確率
- イベント発生確率
- 継承選手から実際に何Lvのコツを取得できるかの予測
- サクセス内での最適行動選択
- 変化球必要経験点そのものの数式推定

本アプリは、

**「必要経験点計算」**

に限定する。

---

# 5. ゲームバージョン管理

データはゲームごとにディレクトリを分ける。

```text
data/
├─ pawapro2024/
│   ├─ config.csv
│   ├─ base_sense_plus.csv
│   ├─ base_normal.csv
│   ├─ blue_abilities.csv
│   ├─ gold_abilities.csv
│   ├─ gold_prerequisites.csv
│   ├─ hint_rules.csv
│   ├─ breaking_cache_sense_plus.csv
│   └─ breaking_cache_normal.csv
│
├─ pawapro2026/
│   └─ ...
│
└─ pawapro2028/
    └─ ...
```

画面上でゲームバージョンを選択する。

```text
パワプロ2024-2025
パワプロ2026-2027
...
```

選択したゲームに対応するCSVのみロードする。

---

# 6. CSV共通仕様

文字コード：

```text
UTF-8
```

Excel編集を考慮してUTF-8 BOM付きも許容する。

区切り：

```text
,
```

数値：

- 整数
- 0以上
- 空欄と0を区別する

空欄：

```text
値が存在しない / 対象外
```

0：

```text
必要経験点が0
```

IDは日本語名称とは別に英数字で保持する。

例：

```text
power_hitter
average_hitter
archartist
contact
power
velocity
```

表示名称変更があっても内部ロジックへ影響させない。

---

# 7. 基礎能力データ

## 7.1 ファイル

```text
base_sense_plus.csv
base_normal.csv
```

センス○と通常状態を別データとして管理する。

MVPでは投手について、

```text
sense_plus
```

を標準利用する。

将来通常状態のデータが揃えば、

```text
normal
```

へ切り替え可能とする。

---

# 8. 基礎能力CSV仕様

```csv
player_type,ability_id,from_value,to_value,muscle,agility,technique,breaking,mental
pitcher,velocity,130,131,10,0,5,0,0
pitcher,velocity,131,132,10,0,5,0,0
pitcher,control,40,41,0,0,4,0,3
fielder,contact,40,41,1,0,5,0,7
```

## カラム定義

| カラム | 内容 |
|---|---|
| `player_type` | `pitcher` / `fielder` / `common` |
| `ability_id` | 能力ID |
| `from_value` | 上昇前 |
| `to_value` | 上昇後 |
| `muscle` | 筋力 |
| `agility` | 敏捷 |
| `technique` | 技術 |
| `breaking` | 変化球 |
| `mental` | 精神 |

原則として、

```text
to_value = from_value + 1
```

とする。

弾道のみ、

```text
1→2
2→3
3→4
```

のように扱う。

---

# 9. 基礎能力計算

例：

```text
現在球速：130
目標球速：133
```

CSVから、

```text
130→131
131→132
132→133
```

の3行を取得する。

各行の経験点を加算する。

疑似コード：

```ts
function calculateBaseAbility(
  abilityId,
  currentValue,
  targetValue,
  data
): ExpVector {

  if (targetValue < currentValue) {
    throw new ValidationError();
  }

  let total = zeroVector();

  for (
    let value = currentValue;
    value < targetValue;
    value++
  ) {
    const row = findRow(
      abilityId,
      value,
      value + 1
    );

    if (!row) {
      throw new MissingDataError();
    }

    total = add(total, row.cost);
  }

  return total;
}
```

基礎能力については、

**センス○の倍率計算をプログラム内で行わない。**

センス○CSVに記録された実際の値を使用する。

---

# 10. 青特殊能力

青特殊能力については、

**コツLv0＝コツ未所持状態**

の必要経験点をCSVへ記録する。

コツによる軽減のみプログラム側で計算する。

---

# 11. 青特殊能力CSV

```csv
ability_id,display_name,player_type,ability_type,from_state,to_state,muscle,agility,technique,breaking,mental
power_hitter,パワーヒッター,fielder,binary,NONE,ON,240,15,68,0,8
average_hitter,アベレージヒッター,fielder,binary,NONE,ON,23,38,195,0,83
chance,チャンス,fielder,rank,D,C,0,16,28,0,100
chance,チャンス,fielder,rank,C,B,0,20,35,0,125
chance,チャンス,fielder,rank,B,A,0,24,42,0,150
```

## ability_type

```text
binary
rank
```

### binary

```text
NONE
↓
ON
```

### rank

```text
G
↓
F
↓
E
↓
D
↓
C
↓
B
↓
A
```

ランク能力についても、

**各遷移の実経験点をCSVへ保存する。**

プログラム側で、

```text
G→F × 4 = D→C
```

などのゲーム固有ルールを持たせない。

これにより新作の仕様変更にも対応する。

---

# 12. コツ倍率CSV

```text
hint_rules.csv
```

例：

```csv
ability_type,hint_level,multiplier,rounding
blue,0,1.00,floor
blue,1,0.70,floor
blue,2,0.50,floor
blue,3,0.40,floor
blue,4,0.30,floor
blue,5,0.20,floor
gold,0,1.00,floor
gold,1,0.70,floor
gold,2,0.50,floor
gold,3,0.40,floor
gold,4,0.30,floor
gold,5,0.20,floor
```

倍率はコードへハードコードしない。

将来検証により倍率や丸め方式が異なることが判明した場合、

```text
hint_rules.csv
```

のみ変更する。

---

# 13. 青特コツ計算

コツなし経験点：

```text
Base
```

コツLv倍率：

```text
Rate
```

各経験点カテゴリについて、

```text
Cost = floor(Base × Rate)
```

とする。

各経験点を個別に計算する。

例：

```text
筋力
敏捷
技術
変化球
精神
```

をそれぞれ独立して丸める。

---

# 14. センス○と青特

青特マスタは原則として、

```text
センスなし
＋
コツLv0
```

の値を保持する。

センス○補正については、

```text
config.csv
```

で管理する。

例：

```csv
key,value
special_sense_plus_multiplier,0.90
special_sense_plus_rounding,floor
```

計算：

```text
青特基礎値
× コツ倍率
× センス倍率
↓
指定丸め
```

初期実装では、

```ts
floor(
  baseCost
  * hintMultiplier
  * senseMultiplier
)
```

を使用する。

将来実測と異なる場合はルールを変更可能な構造とする。

---

# 15. 金特殊能力の基本方針

金特については、

**下位青特を取得済みの状態**

でゲーム画面に表示された経験点を計測する。

例：

```text
パワーヒッター取得済み
↓
アーチスト取得
```

この状態で表示された必要経験点を、

```text
アーチスト固有の取得経験点
```

として記録する。

したがって最終的な必要経験点は、

```text
下位青特取得費
+
金特取得費
```

とする。

---

# 16. 金特依存関係

ファイル：

```text
gold_prerequisites.csv
```

例：

```csv
gold_id,lower_blue_id,required_state
archartist,power_hitter,ON
artist_hit,average_hitter,ON
clutch_master,chance,A
doctor_k,strikeout,ON
```

アーチストを目標に設定した場合、

アプリは自動的に、

```text
パワーヒッター
+
アーチスト
```

を必要能力として扱う。

ユーザーがパワーヒッターも目標能力へ明示指定していても、

**重複計算しない。**

---

# 17. 金特実測データ

```text
gold_abilities.csv
```

例：

```csv
ability_id,display_name,player_type,hint_level,sense_mode,muscle,agility,technique,breaking,mental,data_type
archartist,アーチスト,fielder,1,sense_plus,100,10,50,0,20,measured
archartist,アーチスト,fielder,3,sense_plus,60,6,30,0,12,measured
doctor_k,ドクターK,pitcher,1,sense_plus,50,0,100,70,40,measured
```

## data_type

```text
measured
estimated
```

CSVの恒久マスタには原則、

```text
measured
```

のみ保存してよい。

`estimated`は実行時生成でもよい。

---

# 18. 金特の未計測コツLv推定

金特全能力についてLv1～Lv5を実測する必要はない。

実測されていないLvについて、

```text
hint_rules.csv
```

の倍率を利用して推定する。

---

# 19. 金特推定の優先順位

対象：

```text
アーチスト
コツLv4
```

の場合、

### 1位

Lv4実測値が存在する。

→ 実測値をそのまま使用。

### 2位

Lv4はないが他Lvの実測値が存在する。

→ 実測値から基礎コストを逆算しLv4を推定。

### 3位

その金特について実測値が1件も存在しない。

→ 計算不可。

```text
GOLD_DATA_MISSING
```

とする。

他の金特から推定してはいけない。

---

# 20. 金特基礎コスト逆算アルゴリズム

金特の各経験点カテゴリについて、

仮想的なコツLv0基礎値：

```text
B
```

を求める。

実測したLv：

```text
k
```

倍率：

```text
Rk
```

実測値：

```text
Mk
```

とする。

理論上、

```text
Mk = floor(B × Rk)
```

となるBを探索する。

---

# 21. B探索方法

経験点カテゴリごとに、

```text
B = 0 ～ 10000
```

を総当たりする。

候補Bについて、

```text
推定値 = floor(B × Rk)
```

を計算する。

実測値との差：

```text
error = abs(推定値 - 実測値)
```

を計算する。

複数の実測Lvが存在する場合：

```text
totalError =
Σ abs(
  floor(B × Ri)
  - Mi
)
```

とする。

`totalError`が最小となるBを採用する。

同点の場合：

```text
小さいB
```

を採用する。

---

# 22. 金特推定値計算

推定対象Lv：

```text
t
```

倍率：

```text
Rt
```

とすると、

```text
EstimatedCost =
floor(B × Rt)
```

とする。

5種類の経験点それぞれについて独立してBを求める。

---

# 23. 金特推定信頼度

結果には必ず以下を付与する。

| 状態 | 表示 |
|---|---|
| 対象Lv実測あり | `実測` |
| 他Lv2件以上から推定 | `高信頼推定` |
| 他Lv1件から推定 | `推定` |
| 推定不能 | `データ不足` |

例：

```text
アーチスト Lv3
筋力 75
敏捷 5
技術 42
精神 10

[実測]
```

または、

```text
アーチスト Lv4
筋力 56
敏捷 4
技術 31
精神 7

[推定]
```

と表示する。

---

# 24. 実測値優先ルール

後からLv4を実測した場合、

それまでのLv4推定値は使用しない。

必ず、

```text
measured
>
estimated
```

とする。

同一条件の実測行が複数存在する場合はCSVエラーとする。

---

# 25. 金特取得計算例

目標：

```text
アーチスト
```

現在：

```text
パワーヒッターなし
```

入力：

```text
パワーヒッター コツLv4
アーチスト コツLv2
```

計算：

```text
① パワーヒッター取得費
   青特Lv0基礎値
   × Lv4倍率

② アーチスト取得費
   gold_abilities
   Lv2実測値
   または推定値

③ ①＋②
```

現在すでにパワーヒッター所持なら、

```text
① = 0
```

とする。

---

# 26. 変化球についての基本方針

変化球については、

**完全な数式化を行わない。**

理由は、

- 総変化量
- 所持球種数
- 現在の変化量
- 新規球種取得
- 球種
- 取得順

など複数条件によって必要経験点が変動するため。

したがって、

**作成する選手ごとに必要な変化球構成だけ実測する**

ことを基本運用とする。

---

# 27. 変化球入力方式

2方式を用意する。

## A. 一括実測方式

対象選手について、

```text
変化球完成までに必要だった
筋力
敏捷
技術
変化球
精神
```

の合計だけ入力する。

最も簡単な方式。

例：

```text
スライダー4
カーブ3
フォーク5

必要経験点：
筋力 0
敏捷 0
技術 450
変化球 1280
精神 100
```

この値をその選手計画の変化球経験点として使用する。

---

# 28. 一括変化球CSV

```text
breaking_plan.csv
```

```csv
plan_id,mode,sense_mode,muscle,agility,technique,breaking,mental
ace001,aggregate,sense_plus,0,0,450,1280,100
```

この方式では再利用性は低いが、

**入力負担を最小化できる。**

---

# 29. 変化球ステップ実測方式

必要に応じて、

取得手順ごとに記録する。

```csv
plan_id,seq,pitch_type,from_level,to_level,total_break_before,pitch_count_before,sense_mode,muscle,agility,technique,breaking,mental
ace001,1,slider,1,2,1,1,sense_plus,0,0,10,50,0
ace001,2,slider,2,3,2,1,sense_plus,0,0,12,60,0
ace001,3,curve,0,1,3,1,sense_plus,0,0,25,110,0
```

---

# 30. 変化球キー

再利用する場合、少なくとも以下を一致条件とする。

```text
game_version
sense_mode
pitch_type
from_level
to_level
total_break_before
pitch_count_before
```

`pitch_count_before`は、

**変化球取得前に所持している変化球系統数**

とする。

ストレートは含めない。

---

# 31. 変化球共通キャッシュ

選手作成時に入力されたステップ実測データは、

```text
breaking_cache_sense_plus.csv
```

または、

```text
breaking_cache_normal.csv
```

へ登録可能とする。

これにより、

初回：

```text
未測定
↓
実測入力
```

2回目以降：

```text
過去データあり
↓
自動入力
```

となる。

---

# 32. 変化球データ優先順位

以下の順番で使用する。

```text
1. 選手計画の一括実測値
2. 選手計画のステップ実測値
3. 共通キャッシュ完全一致
4. 手動入力要求
```

変化球については、

**推定値を自動生成してはいけない。**

データがなければ、

```text
変化球経験点が未入力です
```

と表示する。

---

# 33. 選手計画データ

1人の作成予定選手を、

```text
Plan
```

として管理する。

例：

```ts
interface PlayerPlan {
  id: string;
  name: string;

  gameVersion: string;
  playerType: "pitcher" | "fielder";
  senseMode: "normal" | "sense_plus";

  currentBase: BaseStats;
  targetBase: BaseStats;

  currentSpecials: SpecialState[];
  targetSpecials: SpecialTarget[];

  breakingPlan?: BreakingPlan;
}
```

---

# 34. 特殊能力入力

各能力について以下を保持する。

```ts
interface SpecialTarget {
  abilityId: string;

  currentState: string;
  targetState: string;

  hintLevel: number;
}
```

金特の場合、

```ts
interface GoldTarget {
  abilityId: string;

  hintLevel: number;

  lowerAbilityHintLevel: number;
}
```

下位青特のコツLvと金特のコツLvは別々に指定可能とする。

---

# 35. 画面仕様

## 画面1：選手設定

入力：

```text
ゲーム
選手名
投手 / 野手
センス○あり / なし
```

MVPでは投手の初期値：

```text
センス○あり
```

---

# 36. 画面2：基礎能力

表形式とする。

| 能力 | 初期値 | 目標値 | 必要経験点 |
|---|---:|---:|---:|
| 球速 | 130 | 155 | 自動 |
| コントロール | 35 | 80 | 自動 |
| スタミナ | 40 | 85 | 自動 |

入力変更時に再計算する。

---

# 37. 画面3：特殊能力

検索可能な一覧形式。

例：

```text
[検索] 奪三振

☑ 奪三振
  コツLv [3]

☑ ドクターK
  金特コツLv [2]
  下位：奪三振
```

金特選択時は下位能力を自動表示する。

---

# 38. 画面4：変化球

目標構成を入力する。

例：

```text
スライダー 4
カーブ     3
フォーク   5
```

その下に、

```text
変化球経験点
```

を表示。

データがない場合：

```text
この変化球構成は未計測です。

[一括経験点を入力]
[ステップごとに入力]
```

とする。

---

# 39. 画面5：計算結果

最上部：

| 種別 | 必要経験点 |
|---|---:|
| 筋力 | 1,820 |
| 敏捷 | 430 |
| 技術 | 2,240 |
| 変化球 | 1,650 |
| 精神 | 1,310 |
| **合計** | **7,450** |

---

# 40. 結果内訳

```text
■ 基礎能力
球速130→155
  筋力 900
  技術 500

コントロール35→80
  技術 450
  精神 300

■ 青特殊能力
奪三振 Lv3
  筋力 25
  技術 57
  変化球 36
  精神 25

■ 金特殊能力
ドクターK Lv2
  筋力 44
  技術 102
  変化球 64
  精神 44
  [推定]

■ 変化球
スライダー4
カーブ3
フォーク5

  技術 450
  変化球 1,280
  精神 100
  [実測]
```

---

# 41. 計算結果ステータス

計算結果全体について、

```text
確定
推定含む
未完成
```

の3状態を持つ。

### 確定

すべて実測または確定マスタ。

### 推定含む

金特推定値を1件以上使用。

### 未完成

必要なCSVデータまたは変化球入力が不足。

---

# 42. エラー処理

データ不足を0として計算してはいけない。

代表エラー：

```text
BASE_DATA_MISSING
BLUE_DATA_MISSING
GOLD_DATA_MISSING
BREAKING_DATA_MISSING
INVALID_CSV
DUPLICATE_DATA
INVALID_TARGET
```

例：

```text
球速154→155の経験点データがありません。
base_sense_plus.csvを確認してください。
```

---

# 43. CSVロード時検証

起動またはゲーム切替時に以下を検査する。

- 必須カラム存在
- 数値項目が整数
- 負値なし
- ability_id重複チェック
- 基礎能力遷移重複チェック
- 金特同一Lv重複チェック
- prerequisite参照先存在
- hint_levelが0～5
- sense_mode妥当性
- from/to妥当性

異常CSVを黙って読み込んではいけない。

---

# 44. 計算処理順

最終計算は以下の順番で行う。

```text
① 基礎能力
↓
② 青特殊能力
↓
③ 金特の下位青特を自動追加
↓
④ 青特重複排除
↓
⑤ 青特必要経験点計算
↓
⑥ 金特必要経験点
   実測 or 推定
↓
⑦ 変化球経験点
↓
⑧ 全カテゴリ合計
```

---

# 45. 金特下位能力の重複防止

例：

ユーザー指定：

```text
パワーヒッター
アーチスト
```

内部では、

```text
パワーヒッター
```

を1回だけ取得する。

アーチストの前提能力だからといって、

```text
パワーヒッター ×2
```

になってはいけない。

---

# 46. 計算エンジンの主要インターフェース

```ts
interface ExpVector {
  muscle: number;
  agility: number;
  technique: number;
  breaking: number;
  mental: number;
}
```

```ts
interface CalculationResult {
  total: ExpVector;

  base: CalculationItem[];
  blue: CalculationItem[];
  gold: CalculationItem[];
  breaking: CalculationItem[];

  status:
    | "confirmed"
    | "estimated"
    | "incomplete";

  warnings: string[];
}
```

---

# 47. CalculationItem

```ts
interface CalculationItem {
  category:
    | "base"
    | "blue"
    | "gold"
    | "breaking";

  id: string;
  displayName: string;

  cost: ExpVector;

  source:
    | "master"
    | "measured"
    | "estimated"
    | "manual";

  detail?: string;
}
```

---

# 48. CSVキャッシュ

ロードしたCSVはアプリ起動時にメモリへ展開する。

検索を毎回CSVファイルへ行わない。

推奨内部構造：

```ts
Map<string, BaseCost>
Map<string, BlueAbility>
Map<string, GoldAbility[]>
Map<string, GoldPrerequisite>
Map<string, BreakingCost>
```

---

# 49. データキー生成

例：

基礎能力：

```text
pitcher|velocity|130|131
```

金特：

```text
archartist|3|sense_plus
```

変化球：

```text
sense_plus|slider|2|3|5|2
```

のように一意キー化する。

---

# 50. 新作対応

パワプロ新作発売時に、

プログラムコードを変更せず、

```text
data/pawapro2026/
```

を追加する。

必要なCSVを配置。

```text
base_sense_plus.csv
base_normal.csv
blue_abilities.csv
gold_abilities.csv
gold_prerequisites.csv
hint_rules.csv
```

画面のゲーム選択へ追加する。

原則としてこれだけで対応可能とする。

---

# 51. CSVインポート

標準データとは別に、

ユーザーがCSVを読み込んで上書きできる機能を用意する。

例：

```text
[基礎能力CSVを読み込む]
[青特CSVを読み込む]
[金特CSVを読み込む]
[変化球CSVを読み込む]
```

インポートしたデータは標準データより優先する。

---

# 52. データ出力

以下をCSVまたはJSONでエクスポート可能とする。

```text
選手計画
計算結果
変化球実測値
金特実測値
```

将来的な再利用を容易にする。

---

# 53. 保存

MVPではブラウザローカル保存でよい。

候補：

```text
localStorage
IndexedDB
```

大量データになる場合はIndexedDBを推奨。

サーバーは不要。

---

# 54. 推奨技術構成

Webアプリとして実装する場合の参考構成：

```text
Frontend
React
TypeScript

Build
Vite

CSV
Papa Parse等

State
Zustand / React Context等

Persistence
IndexedDB
```

ただし計算エンジンはUIフレームワークから独立させる。

推奨構成：

```text
src/
├─ domain/
│   ├─ calculator/
│   ├─ models/
│   └─ estimator/
│
├─ data/
│   ├─ csvLoader/
│   └─ repositories/
│
└─ ui/
```

---

# 55. 必須テスト

## 基礎能力

現在：

```text
130
```

目標：

```text
132
```

の場合、

```text
130→131
131→132
```

のみ加算されること。

---

## 青特

コツLv2の場合、

```text
CSV基礎値 × Lv2倍率
```

が各経験点ごとに正しく計算されること。

---

## ランク青特

現在：

```text
チャンスD
```

目標：

```text
チャンスA
```

の場合、

```text
D→C
C→B
B→A
```

のみ加算されること。

---

## 金特・実測値

対象Lvの実測データが存在する場合、

推定処理を行わず実測値を使用すること。

---

## 金特・推定値

Lv1のみ実測、

Lv3未測定の場合、

Lv1から仮想基礎値を逆算しLv3を算出すること。

結果に、

```text
estimated
```

を設定すること。

---

## 金特前提能力

アーチスト選択時、

パワーヒッターが自動追加されること。

すでに所持している場合は青特経験点0となること。

---

## 重複

```text
パワーヒッター
アーチスト
```

を同時指定しても、

パワーヒッターを2回計算しないこと。

---

## 変化球

一括実測値が存在する場合、

ステップキャッシュより一括実測値を優先すること。

---

## 変化球未登録

変化球データがない場合、

0として計算せず、

```text
BREAKING_DATA_MISSING
```

とすること。

---

## ゲーム切替

```text
2024
↓
2026
```

へ切り替えた場合、

2026ディレクトリのCSVのみ使用されること。

---

# 56. 完了条件

MVP完成条件を以下とする。

- 初期基礎能力を入力できる
- 目標基礎能力を入力できる
- CSVから基礎能力経験点を計算できる
- 青特を選択できる
- 青特コツLvを指定できる
- ランク青特を計算できる
- 金特を選択できる
- 下位青特を自動追加できる
- 金特コツLvを指定できる
- 金特実測値を利用できる
- 未実測金特を推定できる
- 推定値と実測値を区別できる
- 変化球経験点を手動入力できる
- 変化球ステップデータを再利用できる
- 5種類の経験点合計を表示できる
- 各能力別の内訳を確認できる
- データ不足を検出できる
- ゲームデータをCSVで差し替えられる
- 新作をコード変更なしで追加できる

---

# 57. 最重要設計原則

このアプリでは、

**「不明なゲーム仕様をコードで推測しない」**

ことを最優先とする。

基礎能力：

```text
CSV実測値
```

青特：

```text
基礎値＋明確なコツ規則
```

金特：

```text
実測優先
↓
不足Lvのみ推定
↓
推定であることを表示
```

変化球：

```text
作成する選手ごとに実測
↓
過去実測があれば再利用
↓
不明なら入力を要求
```

という役割分担とする。

これにより、

**「2024-2025で正確に使える」**
と同時に、

**「2026-2027以降もCSV更新だけで長期間使える」**

ことを本アプリの設計目標とする。